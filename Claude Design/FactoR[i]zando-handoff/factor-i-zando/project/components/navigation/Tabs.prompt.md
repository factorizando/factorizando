Pestañas de subrayado para cambiar de vista dentro de una materia (Lecciones · Práctica · Progreso).

```jsx
<Tabs items={[{id:'lec',label:'Lecciones',count:14}]} value={tab} onChange={setTab} accent="var(--fx-coral)" />
```
