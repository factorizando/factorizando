Botón cuadrado de un solo glifo o icono; siempre con `label` para lectores de pantalla.

```jsx
<IconButton label="Cerrar" variant="ghost">×</IconButton>
```

Tamaños 36/44/52px. Nunca lo uses como acción principal de una pantalla.
