Progreso de una lección, unidad o racha semanal.

```jsx
<ProgressBar value={7} max={10} label="Progreso" valueLabel="7 / 10" color="var(--fx-math)" />
```

Siempre acompáñala de un valor numérico: el color por sí solo no comunica avance.
