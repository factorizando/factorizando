Campo de texto con etiqueta, ayuda y error. Para respuestas numéricas o fórmulas activa `mono`.

```jsx
<Input label="Tu respuesta" mono suffix="cm" hint="Redondea a un decimal" />
```

El error se muestra en texto ámbar oscuro (#8A6A00) — nunca solo con el color del borde.
