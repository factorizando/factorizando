Opción única dentro de un grupo (`name` compartido). Para respuestas de opción múltiple en ejercicios, prefiere tarjetas seleccionables con este control dentro.

```jsx
<Radio name="q1" label="24 = 2³ × 3" checked={v === 'a'} onChange={...} />
```
